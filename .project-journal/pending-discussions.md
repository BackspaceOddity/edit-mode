# Pending discussions — edit-mode

## Discussed, not approved
_(picked up by next /resume — surface for re-decision or drop)_

- [2026-06-03 18:50] IIFE-first convergence to canonical package | context: lift BSO Website `lib/proposal-workspace/chrome.ts` (the live, most-complete Edit Mode IIFE) into THIS repo as `buildScript(config)` with configurable CSS-var tokens, so every project sources one implementation instead of forking. Recommended + sized ~3.5h. Yegor chose to build the two features first; convergence not yet given a go-ahead. React components in `src/` would become a thin wrapper.

## Approved, awaiting Linear filing
_(filled only if /wrap detects approved-without-issue and fails to file — anomaly state)_

- (none)

---
_Last updated by /wrap on 2026-06-03. Cleared by /resume after surfacing._
